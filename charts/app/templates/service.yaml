apiVersion: v1
kind: Service
metadata:
  name: {{ include "push-to-helm-repo.fullname" . }}
  labels:
    {{- include "push-to-helm-repo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "push-to-helm-repo.selectorLabels" . | nindent 4 }}

